<h1>All Workers</h1>
<div>
  <% @workers.each do |worker| %> 

    <h4>
      <%= worker.name %> | <%= worker.email %>
    </h4>

    <%= link_to "Edit", edit_worker_registration_path %>
    <%= link_to "Sign Out", destroy_worker_session_path, method: :delete %>

  <% end %>
</div>
